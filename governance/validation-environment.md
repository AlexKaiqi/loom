# 模型验证配置

模型端点、模型名与凭据文件位置由操作者在可信主机配置。凭据放在仓库外的用户受限目录（目录 0700、文件 0600），不传入任务沙箱，不放入源码、请求证据或镜像。

`lore_provider` 负责主机侧 HTTP 与预算准入。公开仓库没有默认真实代理、API key 或可直接使用的完整端到端测试配置；请按 [开发说明](../docs/development.md)重建验证环境。固定响应测试与真实模型测试必须分别标记。
