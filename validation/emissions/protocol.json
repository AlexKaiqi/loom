{
  "scope": "Actual R/E/NATS plus ordinary archive/source resolver fixtures; no new Engine, Pi or F publication claims",
  "path": ".lore/emit-requests.jsonl",
  "limits": {
    "total_bytes": 65536,
    "records": 64,
    "line_bytes": 8192,
    "new_records": 8
  },
  "cases": [
    {
      "id": "EM01-local-helper",
      "expected": "Actual helper creates only ordinary .lore/request file, independent ordinal/data bytes, LOCAL_STAGED not E acceptance; all data retained in fixture archive."
    },
    {
      "id": "EM02-delta-input",
      "expected": "Only appended record sent once; true original NATS bytes/application origin/source and stable ID; repeat same ID no PUB; old input immutable; new explicit next input includes original sequence; zero delta zero calls."
    },
    {
      "id": "EM03-related-rejections",
      "expected": "No new NATS PUB/R event rows on wrong domain/grant/binding/effect/source/pub, changed base prefix, torn/duplicate/forged JSON, links, bytes/count excess; changed same ID payload reaches actual R conflict without another PUB."
    },
    {
      "id": "EM04-original-delivery-cuts",
      "expected": "Drop actual PubAck => UNKNOWN then same-ID query confirms without another PUB; after mark-before-PUB cut => issued UNKNOWN stays no PUB; original accepted-before-mark may dispatch exactly once. No new IDs or model."
    },
    {
      "id": "EM05-definite-negative",
      "expected": "Fixed actual 1024-byte NATS stream rejects >1024 event with explicit original REJECTED; repeat negative no PUB, no event stored, real error raw retained."
    }
  ],
  "dependency_gate": "design/g4/r-gate.json",
  "budget": {
    "nats_servers_max_concurrent": 1,
    "normal_operation_seconds": 5,
    "total_seconds": 60,
    "new_events_upper_bound": 16
  }
}
