# M04: saved boundary, zero task residence, fresh Session query

**Preparation only; no Engine, NATS, HTTP or model execution.** Original M04 requires three repeats. Its `system/contract.md:31` explicitly says a **new container** queries the saved result with zero additional calls. v5:226,326,336 requires releasing task processes/sandboxes then continuing from the existing Session. Fresh `Runtime.query` alone is insufficient: `entry-contract.md:18` deliberately makes it R-only, with no Node execution. Preserve both meanings by observing two separate explicit actions.

Use the existing `Flow.result_saved` hook (`flow.py:100–101`). At this point the real S final original has been confirmed, X sealed/released, delivery recorded, and R saved the original result. The parent is `decide`, with a result but no accepted decision; this is a real nonempty pending responsibility. A fixed single localhost final-text response is enough to create this boundary without a tool. No task semantics or model success is being tested here.

For each of three independent ordinary fixtures:

1. Run actual same-config assembly using the existing M03/assembly service fixtures. On the original parent's `Flow.result_saved`, fsync the cut record and stop the Runtime child externally using M03's exact PID/PGID process helper. Do not unwind or fabricate a saved result. The parent verifies all original S/X CIDs and volumes are **404 before any new Runtime starts**, while original S input/result and R `decide` remain. No test cleanup may produce the passing absence observation.
2. Fresh process, identical config and IDs, `assemble` then original `Runtime.query` only (no `open`, register, start or drive). Compare the complete original R tables/auxiliary records, X/S/F/provider files and domains before/after using existing M03 fact reads. There must be zero new HTTP, tool, Node dispatch, ownership confirmation or input-range selection. Startup reentry loads the old trusted originals.
3. A separate fresh process explicitly calls the existing `SessionService.invoke(action='query')`, not `Runtime.query`. Its request uses the original confirmed drive's normal SnapshotStore envelope and full R input/Harness/capability/source binding. A new trusted execution ID is solely the new X read environment; it does not replace the original operation ID or create a new provider/tool identity. Existing SessionPlans and X validators perform all authorization. Observe a new CID/volume/ExecID and live Engine inspect, different from every old released object. Compare its restored Pi JSONL/result bytes and complete original Node input binding to the old S owner. Query must return the original saved frame/result with no `provider.request` or `tool.request`; original HTTP/files/R transport rows/tool records must not increase. It may create the normal new read environment, immutable F config/view, X receipts and S confirmation; those are not claimed as zero filesystem writes. Its original result/input and every preexisting owner file must remain unchanged. Parent `decide` and its result remain unchanged during this read. Finish with actual seal/release and new CID/volume 404.
4. Explicitly finish the original pending decision through ordinary Runtime `drive_until` only after its real lease expires, using M03's original monotonic wait. This reuses the already saved external boundary/control body, accepts/applies that original stop decision exactly once, and must add zero provider/tool calls or Node executions. Do not change lease or invoke a model to reconstruct the result. This proves continuation of the pending responsibility, not merely a final all-settled query fixture.

Existing S27 merged evidence (especially S017/S026) supports saved-result/zero-effect query semantics; Startup reentry's independently accepted CSR01–04 supports original-source loading and read-only query after physical F changes. Neither gate claims this new assembled M04 sequence has run. The new explicit Node query is required by the original M04 scope; if current trusted SessionPlans cannot authorize it, report the real gap, without substituting fixture authority or only querying SQLite.

Reuse `runtime_recovery.fixtures` ordinary directories and fixed response, `execute.wait_cut/finish`, `facts.snapshot` original-store reads, existing EngineRead/engine_hook, `runtime_observer_sources.raw_value/tar_files`, and `components.s.oracle.pi_jsonl`. Do not create a new observer, reference resolver, ownership ledger, public API or permissions. All three cases use one fixed response each, zero real models, no tool dispatches, one sequential original slot (3 objects/768 MiB bound), and the original M03 180-second finite envelope. Execution requires a later explicit root Engine window. Preserve first failure and mark remaining cases NOT_RUN.
