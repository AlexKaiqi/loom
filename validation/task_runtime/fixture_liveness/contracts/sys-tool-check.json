{
  "kind": "sys.tool.check",
  "producer": "runtime",
  "note": "Liveness reminder: the call is still running. It never kills anything by itself; what to do is the harness's policy. Backoff doubles up to the runtime cap; check_seq is 1-based per call.",
  "payload": {
    "call_id": "string (<round_id>:<step>)",
    "check_seq": "integer (1-based per call)",
    "elapsed_ms": "integer since the call started",
    "silent_for_ms": "integer since the last observed output byte",
    "backoff_ms": "integer (cadence until the next check)"
  }
}
