# Original M02 finite assembly preparation

Scope: original M02 in design/g3/system/cases.json and contract.md; five adversarial boundary families, three repeats each. cases.json defines the exact limited attacks and independent expected facts before code execution. Existing M01 configuration, bootstrap, local HTTPFixture, NATS Server and independent read helpers are reused. No new Runtime, policy/permission framework, model loop, dependency or original-case change.

Authorization fixture: each case owns one Surface and one Workspace, registered read/write for operator in its own namespace. A sibling private directory, generated canary bytes and fixed host authority JSON are outside both task domains. The trusted callback supplies no credential. The FD case alone makes a generated canary FD/environment value inheritable; it never opens or passes real credentials. Runtime target receives only its existing read-only input/history mount; no new cross-domain alias is created.

Target and identity tests act through real assembled R/X owners and preserve original requests/exceptions plus original object/row before-after facts. They are not a native-Harness-only denial claim. FD/event/stdout run the actual fixed localhost provider -> Pi -> Runtime -> native Shell -> X/F/R chain. The event attack writes raw malicious outbox bytes, bypassing the CLI; only the real source-bound trusted rejection counts. Stdout remains ordinary data even when it resembles a decision. Existing independent component passes are prerequisites, not combined M02 evidence.

Run preparation with `research/.venvs/runtime-research/bin/python -B validation/runtime_security/run.py --batch FRESH`; it writes fixtures and MISSING/0 only. After root independently reviews and grants an execution window, add --execute. There is no real-proxy mode. Raw input/output, original SQLite/JetStream/X files, exact Engine CID/exec queries and protected file/control hashes determine checks. Cleanup occurs after assessment and cannot turn failure into PASS. Actual imports use shared frozen source with before copies/after hashes; large fixed dependencies are referenced, not copied.

No Engine, HTTP/NATS, process task or model calls are permitted while preparing this branch. Missing capabilities retain MISSING; no recorded Runtime status, Harness block text or expected exception generated by the test is treated as independent evidence.
