# Original M03 crash recovery preparation

The fifteen cases in cases.json refine only original M03: five specified cuts, three repeats each. No product, original case, M02 or validation/system source is modified. The fixed local HTTP and NATS services run outside the killed Runtime child. Parent records the exact request, cut, OS PID/PGID, kill and original stores before a new process uses the same configuration and original Runtime.query. Query never calls Runtime.open/start/register/drive, confirms an owner, or performs cleanup.

Accept needs only proof of its original committed acceptance. Decision uses a first final-text response and explicit recovery drive to apply the original saved decision. Provider/tool/install first remain issued under read-only query; only after the actual persisted original lease expires may a distinct recovery drive pause them. The original 30 second lease and monotonic clock are never changed. A native Workspace script appends and fsyncs one fixed marker; original effects, exec identities and F directory exchange identities reveal repeats. No external effect is inferred from Runtime replies alone.

Existing assemble, M01 configuration, M02 finite fixture/ordinary-reader helpers, HTTPFixture and NATS Server are reused. Snapshotting existing source proves shared-tree imports before/after, not copied execution. A saved-host bootstrap that cannot read the original F state after a pending exchange is a real missing prerequisite, not permission to recapture current files. No alternate startup or recovery owner is supplied by the test.

Default entry prepares ordinary files and reports MISSING/0. Actual --execute requires a separate root execution window after review. No Engine, HTTP/NATS service, credentials or model runs during preparation. Each actual cut has a 180 second outer bound and exactly zero or one fixed localhost response. All original evidence precedes test cleanup, which selects only the exact original slot owner/CID/volume and cannot promote failure.
