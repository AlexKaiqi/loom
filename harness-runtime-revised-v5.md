# 系统底座（Harness Runtime）

**v5 · 2026-09-12**

## 1. 目标

**核心主目标**

Runtime 与 Harness 彻底拆开，Harness 降级：

- **Runtime 纯化为底座契约**：不内置任何关于“如何驱动模型推进”的策略定义，只提供四项基础契约：**事件底座**（定义事件交互契约规范：包括命名空间、如何发布与获取、核心系统事件）+ **Surface**（工作面，承载推理所需上下文，并在执行与探索中动态变化）+ **任务环境沙箱** + **解释 Harness 并执行推进**。
- **Harness 降级为外部推进定义**：描述 LLM 如何持续推进。基于 `Surface` 与 `Event`，定义：
  - **模型看什么**：定义如何提取并组装模型可见上下文。
  - **怎么推下去**：定义如何推进以及如何描述推进——包括当前 Surface 自身的推进，以及如何声明其他 Surface 的推进依赖（明确何时触发、作用于哪个 Surface、信息与产物如何传递）。

**价值**

- **多策略原生并存与解耦演进**：Runtime 作为完全中立的宿主，不预设任何任务范式。既支持不同任务按需挂载专属 Harness 并发运行，也支持在同类任务上挂载多套策略做对比评估（A/B 测试、影子评测）；在既有底座契约范围内，策略的演进与替换完全不侵入底座。

- **等待期资源归零与极高并发伸缩**：等待期没有任务专属的常驻 Agent 进程与长连接挂起，非执行期任务专属算力与沙箱占用归零（Scale to Zero）。无论任务长程挂起多久，都以持久化工作面与事件等状态静态驻留；事件条件满足时按需拉起，进入等待或非执行期时释放，为超大规模等待任务的驻留与高弹性伸缩提供基础。Runtime 作为共享底座可以常驻。

- **可靠性／长程维：状态持久化与故障后的无损接续**。计算和状态解耦，任务推进所需的上下文、事件与执行记录持久保存。面对沙箱崩溃、系统重启或网络中断，任务不因执行进程消失而丢失已确认的状态，并能够从明确的恢复边界继续推进，保持长程任务的上下文连续性。

## 2. 原则

- **顺应模型训练先验**：交互界面与上下文组织全面对齐 LLM 的预训练认知分布。坚持使用标准的 UNIX/POSIX、代码及通用文件语义与模型交互，绝不发明反直觉的私有 DSL、复杂 Schema 或专属通信协议。
- **优先复用成熟工业基建，不造劣质轮子**：底座核心价值在于契约定义与极简粘合。存储、隔离、版本追溯与执行等底层能力，优先复用成熟的工业级标准与开源基础设施，拒绝闭门造车。
- **确定性与可逆性高于极致性能**： 系统的核心指标是破坏半径严格可控与状态随时无损回滚，优先保证环境隔离的确定性与执行可复现，不为微秒级延迟或单机极限并发牺牲系统确定性。

## 3. 核心概念

**Runtime**：纯粹的系统底座。负责事件交互、Surface 接入、执行环境生命周期、解释 Harness 并落实推进，以及必要的记录、快照与恢复。它不内置任务策略，但负责执行外部定义所需的通用运行机制。

**Harness**：外部推进定义。基于 Surface 与 Event，说明模型看什么、如何消费反馈、下一步如何推进。可以用普通代码、模板及必要配置表达；“降级”指推进策略不再自带另一套事件、环境、持久调度与恢复底座，不是削弱策略的表达能力。

**Surface（工作面）**：“认知推理的工作面”，就是一个目录，由模板文件与 block files 组成，承载目标、Prompt 组装碎片、工作笔记、召回上下文及必要的产物引用，并在执行与探索中动态变化。Git 是版本追溯的候选实现，不是创建工作面的前提；Surface 也不因此变成一套专用 CRUD 服务。

**User Workspace（用户工作空间）**：任务沙箱作用的执行靶场，例如用户代码仓库、数据目录和任务输出目录。它与 Surface 分别承载“模型据以推进的上下文”和“任务实际操作的对象”。

**Event／事实流**：关于系统观察、请求受理或应用声明的可追溯记录。事件服务提供身份、读取、交付及持久性契约；消息正文中的声明不自动成为 Runtime 已确认的事实。事实流不绑定某个日志文件，也不要求把已有局部执行历史复制到另一处。

**Task Environment（任务环境）**：挂载获授权 Workspace、运行具体命令、安装依赖与调用工具的沙箱环境。资源按需分配，进入等待、空闲回收或明确停止时可以释放，必要状态留在环境之外。

**Runtime Shell 与任务沙箱 Shell**：两套明确区分的执行环境，都使用普通 Shell／Python。前者用于获授权的 Surface 内容与运行辅助操作，后者用于任务代码与 Workspace 操作；实际环境、用户和权限由 Runtime 决定。Runtime Shell 不等于守护进程的高权限宿主 Shell。

**Orchestration**：Harness 外部推进定义中涉及多个 Surface 的普通代码，描述哪些事件条件触发哪个 Surface，以及输入和产物如何传递。它是后续可增加的推进定义，不是最简 Harness 的前置依赖，也不要求引入任务图 DSL。

**Namespace**：登记、授权与资源组织的范围。是否同时作为事件排序范围，由具体事件契约说明。

**Step（推进）**：一次有明确输入、模型结果和执行反馈的离散推进。沿用原稿的示意关系：

```text
(Surface_t + 本次可见事件及反馈)
    ── Harness：上下文投影与推进定义 ──> (Context, 推进意图)
    ── Runtime：解释与受控执行 ──────> (Surface_{t+1}, 执行记录, 新事件)
```

这里的“推进意图”（原稿中的 Action）只是泛称，不规定三轨业务 Schema，不把每种文件操作变成一个专用 Action，也不表示整步是确定性函数或原子事务。

**事实归属**：Surface 保存上下文文件；Workspace 保存任务文件；已有局部 Session／执行器继续负责模型交互与局部执行事实；Runtime 保存自己负责的登记、授权、推进关联、事件交付和环境记录。沿用既有底座时，例如 `dsh session` 已维护的局部事实，优先复用其记录与引用，不在跨 Surface 层另建一套竞争性的局部真相。

## 4. 核心不变量与工程决策

本节沿用原稿的区分：**核心不变量是为兑现目标而要求实现保持的约束；工程决策只是根据有限认知提出的实现设想，并非真理。** 前者仍需通过实现验证，后者允许替换。目标与原则不由某个候选产品、目录布局或 CLI 反向决定。

### 4.1 认知与执行双域分立、权限硬隔离与版本无损可逆

**核心不变量**

- **认知与执行分立**：Surface 和 Workspace 的职责、写权限与恢复范围分开。任务沙箱默认不能写 Surface；获授权的 Runtime Shell 可以直接编辑 Surface 内容，但不能因此改写 Runtime 的授权、权威事件记录或其他工作面的内容。
- **写入边界明确**：同一 Surface 的写入不能互相覆盖，共享 Workspace 也必须有自己的写入协调。切换执行者时，旧执行者需要停止或失去实际写能力；不能只改控制记录就假设旧进程已经停止。
- **版本可追溯、回退有范围**：Surface 与 Workspace 分别记录必要版本。回退可以分别选择上下文或任务文件，但恢复组合须明确，不能忘记此前已经发生的执行与外部效果。回退本身留痕，不能把历史伪装成没有发生过。

本原则中的“无损回滚”，针对约定保留范围内可恢复的受管理状态；撤销远端副作用需要对应系统的查询、幂等或补偿能力。恢复已经保存的模型结果，与再次调用模型获得相同结果，也不是一件事。

**工程决策（待验证）**

优先评估 Surface／Workspace 分目录组织、成熟沙箱隔离、明确的进程身份与挂载配置。目录可以平铺，但不强制只能同机、只能零拷贝，或默认整个 Namespace 互相可读。

版本追溯优先复用 Git 或现有快照能力，在需要恢复的边界捕获相关内容与执行引用；不要求每次编辑都调用一个提交 API，也不预先固定每步强制提交。快照需要覆盖实际依赖的内容，而不只是版本库已经跟踪的文件。

首个实现可选择同一 Surface 串行推进、渲染期间不并发修改、Workspace 独占写入。允许普通文件编辑，不代表多文件修改天然具有事务性；未纳入协调的人工编辑也不能被当作不存在。

### 4.2 故障单向阻断与任务沙箱弹性归零（Scale to Zero）

**核心不变量**

- **破坏半径可控**：执行环境的文件、进程、网络、凭证与资源边界由 Runtime 落实，任务错误和资源耗尽不能获得修改 Runtime 控制状态的能力。具体隔离承诺须对应威胁范围和验证结果。
- **计算与状态分离**：任务继续所需的信息不能只存在于沙箱、连接或推进进程的内存中。进入长程等待前，必要状态和后续推进依据必须保存。
- **等待期任务专属资源归零**：等待任务不依赖任务专属 Agent 进程、长连接或沙箱持续存活；共享 Runtime、事件服务与持久存储可以常驻。不能只销毁任务沙箱，却把每个等待任务改成 Runtime 域中的常驻 Agent。
- **按外部定义恢复推进**：满足的是 Harness 所定义的触发条件；Runtime 负责发现相应输入、重新建立执行环境并接续，而不是只看事件名就自行推断业务含义。

**工程决策（待验证）**

复用成熟容器、强化沙箱或微虚拟机。连续活跃执行阶段可以短暂保温；进入等待或非执行期后释放任务资源，不要求每条 Shell 命令结束都立即重建环境。

环境重建依据包括必要的 Workspace 状态、环境版本、依赖与配置。模型密钥和管理句柄不默认下发给任务代码。超时和资源限制由 Runtime 强制执行，取消请求与实际停止分别记录。

“极高并发伸缩”同时需要区分等待任务的驻留规模与活跃任务的吞吐量，具体容量、延迟和成本在后续验收中测量。

### 4.3 决策物理解耦与显式状态推进

**核心不变量**

- **策略定义归 Harness，运行责任归 Runtime**：模型看什么、如何利用反馈、何时继续或暂不继续，由外部定义给出；输入交付、实际执行、资源管理和已受理推进责任的保存，由 Runtime 提供。
- **外部定义不依赖常驻内存**：允许活跃阶段使用进程与缓存，但暂停后能够从持久输入和已确认记录重建必要状态。包含模型调用的推进不要求成为无副作用、输出恒定的纯函数。
- **执行方式通用，环境明确分流**：普通文件编辑、代码构建和数据处理优先使用 Shell／Python；Runtime 在执行前落实相应环境和身份，不通过专用业务动作区分“改笔记”与“跑测试”。
- **多策略共存而不共享破坏范围**：不同 Harness 可以使用相同底座契约。策略对比须固定必要输入并隔离可写资源及外部副作用，不能让影子策略直接修改同一用户资源。

**工程决策（待验证）**

Harness 以普通代码、模板和必要配置登记，Runtime 按约定入口解释并执行。代码可以表达单步与继续条件，不需要新造 DSL；“解释 Harness”也不缩减为启动一个自行承担全部调度与恢复的黑盒 Agent。

优先复用局部 Session 的模型适配、执行记录和恢复能力。若它已经管理模型—工具循环，Harness 通过已有配置或回调定义策略；Runtime 通过适配补齐交接责任，不在外层重做同一局部循环。

同一底座可以支持不同调用粒度。最简参考采用离散单步、串行推进，但不把这种粒度冻结为所有 Harness 的唯一形式。原稿中的“纯函数”实现设想收窄为：必要输入显式、结果可记录、继续位置可恢复。

### 4.4 客观事实留痕与事件驱动推进

**核心不变量**

- **事实受控写入，历史可追溯**：普通任务代码不能直接改写或伪造 Runtime 确认的系统事实。应用声明可以作为声明记录，不因此升级为独立验证结果。
- **确认、顺序与保留范围明确**：事件具备稳定身份，顺序在约定范围内成立，确认具有明确的持久性含义。保留与归档不能静默破坏仍被承诺支持的恢复依据。
- **依赖由外部代码定义**：哪个 Surface 依赖什么事件、使用哪些内容，由 Harness／orchestration 说明。Runtime 执行该定义，不预设父子任务、通用终态或固定 DAG。
- **交付责任可恢复**：事件输入交给哪次推进、结果保存到哪里、后续应否再推进，都应有明确依据；丢一次通知不能永久丢失已经受理的责任。

**工程决策（待验证）**

优先在 NATS JetStream 等成熟持久事件设施之上做必要适配，不自研基于文件锁、末行序号和文件通知的消息系统。具体组件、持久化配置、排序范围和消费机制仍需验证。

面向 Harness 保留文件友好的读端：Runtime 将约定范围的事件注入可读取位置；存储内部不必是文件。`events.jsonl` 可以是输入视图或导出格式，不再是底座的本质定义。

不把“去中心化”或“禁止 DAG”作为中立性的要求。暂不引入多 Surface 编排的最简版本，仍保留以后用普通代码登记推进依赖的扩展位置。

## 5. Runtime 的四项基础契约

四项契约落实前述目标，不是四个必须独立部署的服务，也不是四套新增 CLI。以下定义所需语义；具体方法名、字段全集和传输方式仍属实现选择。

### 5.1 事件底座：如何发布、获取、等待与识别

**发布与校验。** 调用者通过获授权的提交入口发送完整消息。Runtime 检查必要格式、大小、来源权限与系统请求条件后，再接受事件或处理请求。通用事件不要求 Runtime 理解全部业务载荷；系统登记请求需要额外校验对象和绑定。

提交需要有可核对的回执：明确拒绝、已接受／已保存，以及操作确已完成不能混淆；失去回执时可能存在结果不确定。重试关联原请求，同一身份而内容不同应报告冲突。请求身份由调用设施管理，不要求模型凭空编造。

`emit` 可以是唯一的候选薄包装，也可以复用已有调用设施。FD、管道或 socket 可以是内部传输，但“字节已写出”不是 Runtime 校验通过的证明；接收端校验和回执不能因为使用文件式接口而省略。任务沙箱默认不获得 Runtime 控制提交能力，需要上报时另行限权。

**获取与输入注入。** Runtime 在调用 Harness 前，准备本次获授权事件及必要记录的文件视图，并通过明确参数或环境变量告知位置。Harness 可以用 `cat`、`jq`、Python 等读取；不要求模型访问底层消息服务 SDK，也不以“有个工作视图”代替实际路径。

读端必须说明：输入何时完整可读、范围和筛选条件、来源身份、引用在当前环境如何访问，以及历史缺失或权限不足如何报告。参考实现把一次 Step 的输入边界固定，新事件在下一次约定输入中交付；运行中动态注入属于另行定义的能力。

文件是输入视图，不是进度确认。Runtime 保存输入与推进的关联；已经完成的结果和可恢复的待处理责任不能因读取文件、连接断开或重复通知而丢失。局部执行反馈复用对应 Session／执行器记录。

**等待与持续交付。** 事件底座提供历史读取、订阅和恢复消费所需能力；它们可以在 Runtime 内部使用，不因此增加面向模型的查询／等待命令。已发生的目标事件与“下一次输入”需有明确起点，不能因先检查后订阅而漏掉需要处理的输入。

长程等待的条件和继续安排由外部推进代码给出，Runtime 保存并执行其交付责任。等待不需要一个任务进程长期阻塞，也不要求一个 `rt-wait` 原语。具体多 Surface 等待在启用 orchestration 时再展开。

**核心系统事件。** `surface.registered`、`orchestration.registered` 是候选确认事实，分别表示工作面目录或推进定义已完成登记；实际执行开始、退出和环境异常也可以记录为系统观察。事件命名仍待落实，调用者不能通过填写同名字符串伪造 Runtime 的确认。

登记声明可以沿同一提交路径处理，完成后返回登记事实的引用，不需要另造 `spawn`。消息保存成功不自动等于登记完成；注册完成也不自动等于已经启动模型。

`ready / done / fail` 可以由应用自行使用，但不是每个 Harness 都必须接受的 Runtime 业务语义。普通 stdout 或 block 文件中出现控制字样，不自动成为系统请求。

**底层保证。** 实现需声明持久性、读取一致性、排序范围、去重与保留边界。时间戳和流序号不承担全部业务因果关系；普通执行不能改写历史，但归档、删除和恢复期限仍需明确。事件回放只恢复已记录事实，不直接重做其中描述的动作。

### 5.2 Surface：目录、模板与 block files

**创建就是文件操作。** 在已获授权的 Runtime 工作位置用 Bash／Python 创建目录、模板和 blocks，然后登记现有目录。创建尚未登记的新目录，使用已授权的父级工作位置，不要求它先拥有自己的登记。

**登记只负责接入。** Runtime 确认目录身份、所属范围、Harness 绑定与获准执行资源。普通内容编辑不需要重新登记；改变绑定、移动目录或移除已登记路径时，需要明确处理登记关系，不能继续使用失效路径。

**内容由 Harness 组织。** 模板引用并组织 block files，Harness 选择如何渲染。Markdown fence、XML 等只是模型适配方式，不是 Runtime 内置的 block 类型或协议。读取内容与执行代码分开，文件中写着指令并不赋予其控制权限。

**编辑不经过专用 CRUD。** Runtime Shell 在获授权目录直接修改模板、blocks 和辅助文件，不需要 `UpdateScratchpad / PinContext / Commit` 操作。版本捕获由已选择的机制在必要边界完成，普通文件操作不会因登记而变成另一套业务 API。

**文件引用与恢复。** Runtime 提供当前执行环境可访问的位置，任务产物先保存在 Workspace 或获准输出目录；需要时由 Runtime Shell 在 Surface 中记录引用。单 Surface 读取已有文件不以调用 `ready` 为前提。用于历史恢复或跨 Surface 消费的内容须能定位到实际版本，不能只靠后来会变化的路径声称可复现。

### 5.3 任务环境沙箱与双 Shell 执行

**统一调用方式，明确两套环境。** 两套 Shell 都运行普通代码；通用执行入口接收获准目标位置、脚本与必要输入，不需要“认知操作”和“任务操作”两套业务命令。

| 项目 | Runtime Shell | 任务沙箱 Shell |
| --- | --- | --- |
| 主要对象 | 获授权的 Surface、输入视图、运行辅助文件 | 指定 Workspace、任务代码、数据与依赖 |
| 可写范围 | 获授权的 Surface 内容和临时区 | 指定 Workspace、输出区和沙箱临时区 |
| 执行身份 | Runtime 指定的受限工作身份 | Runtime 指定的任务环境身份 |
| 控制能力 | 仅当前范围内获准的提交与调用能力 | 默认不继承 Runtime 控制端点、模型密钥或管理凭证 |

**环境和执行用户由 Runtime 决定。** 调用者只能选择获授权的执行目标；Runtime 在启动进程前落实环境、环境内工作目录、用户和组、挂载、环境变量、允许继承的端点、网络与资源限制。模型不能通过填写 `user=root` 或修改 blocks 改变这些授权。

**目录可以路由，但不是权限来源。** 参考方案登记 Surface／Workspace 目录与执行环境的对应关系，执行前按目标位置解析。不能通过脚本中的 `cd` 切换环境，不能从脚本内容猜测路由，也不能只用字符串前缀代替授权和路径核验。同一位置不能唯一确定环境时，明确选择获准目标；无权、歧义或未登记时拒绝，不回退到宿主执行。

**一次执行固定一个环境。** 在任务沙箱生成报告，再在 Runtime Shell 更新 notes，是两次经同一入口提交的执行，通过获授权的文件和结果引用衔接。端点与开放文件的继承也属于隔离范围，不能仅靠目录不可见来排除控制通道泄漏。

**结果能够核对。** 优先复用现有执行器的身份与结果记录，区分未启动、运行中、确认结束和无法确认；说明真实环境与身份、退出原因和输出引用。部分文件修改、观察连接断开或退出码不能被错误地解释为全无副作用。

**超时、取消与释放。** Runtime 控制执行及其子进程、预算和资源，记录取消请求与实际停止。确认必要文件和结果保存后释放环境；执行结果不确定时先查询或暂停，不盲目重跑。重新准备环境所需的版本、依赖和输入必须可取得。

### 5.4 解释 Harness 并执行推进

**解释的是外部推进定义。** Harness 给出上下文投影、模型接入选择、交互表达、反馈处理和继续规则；Runtime 按约定入口执行这些定义，并落实输入交付、模型／工具调用衔接、执行分流、结果记录和继续调度。通用机制由 Runtime 提供，具体策略不写死在其中。

普通代码可以是定义的载体；底座不需要新的工作流语言，也不能把“运行一个脚本”当作已经满足持久推进。哪个执行阶段已经完成、下一步依据是什么，仍需在对应记录中可恢复。

**输入必须完整可定位。** 至少提供当前 Surface 目录、事件与反馈位置、获准执行目标、当前推进及局部记录引用、必要限制。模型、模板和输出解析规则属于所绑定 Harness；记录相应版本，不在一次已开始的推进中静默换用新策略。

**推进与继续分工明确。** Harness 决定看到某个结果后是否继续及如何继续；Runtime 保存执行结果和已接受的下一步安排，并负责实施。普通事件出现不自动启动模型，Shell 成功也不天然意味所有 Harness 都该再调用模型；最简 Harness 会明确选择自己的继续规则。

**没有通用 Surface 终态要求。** 一次推进系列可以按 Harness 定义给出回答、暂停等待或报告无法继续，Runtime 记录该次调用与执行的实际结果。无需 `rt-done / rt-fail`，也不把退出码或模型自述等同于业务验收，更不因此给目录永久封上终态。

**首次启动与后续编排。** 准备目录并登记后，首个最简样例由外部明确发起；注册本身不隐含启动。多 Surface 需要时才登记 orchestration 代码，定义输入条件、目标 Surface 和信息传递；Runtime 承担其持久交付和执行责任，不让编排程序为了等待长期常驻。

## 6. 极简 Harness（Minimal Baseline）

在 Runtime 负责解释外部定义和执行分流的前提下，最简 Harness 仍只内聚原稿的四项核心定义：**Loop、投影、工具、裁剪**。这里不另造一个 Agent 框架。

参考范围为一个 Surface、一个 Workspace、串行推进，不考虑父子关系或编排拓扑。原文单步映射保留为参考实现，但去掉“模型全过程必须是纯函数”和“靠通用终态原语结束”的要求。

### 6.1 Loop（单步映射与反馈驱动）

**单步定义。** 读取本次 Surface、事件和前序反馈，组织上下文，调用所选模型，并依据本 Harness 的普通代码解释完整输出。实际模型记录与工具调用优先沿用已有局部 Session，不新增另一套局部事实流。

**反馈与继续。** 本参考 Harness 选择：模型提出合法 Shell 请求时，由 Runtime 执行；得到并保存明确结果后，按本策略进入下一步。结果不确定则先核对，不把重新调用模型或重发命令当作恢复。

**本次结束。** 模型给出符合当前交互约定的普通最终回答时，保存并结束本次推进系列；格式错误、截断或未完成请求不是最终回答。轮次与预算限制可以停止继续，但不需要通用 `done / fail` 事件。

**不以常驻循环保存任务。** Loop 是 Harness 的外部策略定义，Runtime 或已适配的局部执行器实施这个循环并保留继续依据。可以在活跃阶段复用进程；已保存的单步边界应允许释放任务进程与沙箱，再由记录继续。不能为了实现最简 Loop，把持久调度和环境恢复又塞回 Harness 私有常驻进程。

### 6.2 投影（物理事实向认知视角的组织）

**工作面投影。** 读取模板和选定 blocks，组装目标、工作笔记和材料引用。Markdown fence 或模型适配格式由 Harness 选择；相关内容更新在下一次明确的渲染中体现。

**事件与反馈投影。** 从 Runtime 注入的文件视图和已有 Session 记录中，选取本轮输入、前序操作与实际结果。无需每次全扫历史，也不要求把整个事件底座直接交给模型。

**工作位置投影。** 明确哪些路径是 Surface 上下文，哪些是 Workspace 执行对象，以及它们如何用于同一 Shell 入口。Surface 的 Git 状态不能冒充 Workspace 的代码状态；引用必须能在对应环境被访问。

### 6.3 工具（契约注入与通用执行）

**双域能力说明。** 告知模型两个获准工作位置及其用途：编辑模板、blocks 使用 Runtime 工作环境；运行项目、测试和数据处理使用任务沙箱。两者都用普通代码，由 Runtime 落实环境和身份。

**交互格式。** 可以复用现有 Shell 工具调用，也可以由 Harness 解析约定的 Bash 代码块；目标位置与脚本必须无歧义，不把任意文本中的代码块或控制字样自动当作执行请求。采用哪一种是 Harness 的适配选择，不是 Runtime 私有 DSL。

**最小额外入口。** 普通内容操作无需新命令。必要的事件或登记提交使用已有受控设施，至多提供一个 `emit` 式薄包装，处理校验、确认和错误反馈；不增加 `spawn / wait / ready / done / fail`、block 编辑或 Surface CRUD 命令。

### 6.4 裁剪（防爆与信息降维）

**输出裁剪。** 对超长执行日志保留有用摘要或首尾片段，标记裁剪并提供获授权的完整来源。原始执行结果继续由其权威组件保存。

**历史选择。** 保留当前目标、必要 blocks、本次输入和继续所需的近期反馈。固定最近 1—3 步可作为实验选项，不作为不可变约束；尤其不能删掉尚未确认的结果后重复执行。

**主动检索。** 不预先展开全量目录树、所有文件和长篇 diff。允许模型在获授权环境中使用普通工具查找，但先让它知道正确路径和执行位置。

裁剪只改变模型可见上下文，不删除持久恢复依据，也不重写已经发生的执行事实。

### 6.5 最小接线示例

以下路径、文件名和调用参数只是一份**待实现的参考接线**，不是新增标准或声称已有的 API。

```text
surfaces/demo/
├── surface.md                 # 模板
└── blocks/
    ├── goal.md
    └── notes.md

workspaces/demo/
└── numbers.json               # 本例输入 [2, 3, 5]

<Runtime 注入的只读输入目录>/
├── invocation.json            # 当前 Surface、推进关联、局部记录引用
├── events.jsonl               # 本 Step 的事件视图，不是权威存储
└── execution-targets.json     # 可用目标与路径说明，不含管理凭证
```

参考方案通过 `RUNTIME_INPUT_DIR` 或等价调用参数告知最后一个目录的位置。模板具体如何引用 blocks，由 Harness 的普通渲染代码约定，不在 Runtime 中发明占位符语言。

**准备与启动：** 外部获授权脚本创建 Surface 和 Workspace 文件，提交登记并取得完成确认，再明确启动这一 Surface 的推进。任务内容为：计算总和、生成报告，并在 `blocks/notes.md` 中记录报告位置。

**执行流程（职责示意，不是已接入底座的程序）：**

```text
Runtime 按最简 Harness 的定义开始一个 Step
    → 准备明确的事件、前序反馈与执行目标输入
    → 执行 Harness 的投影、渲染与裁剪代码
    → 按 Harness 的模型选择和交互约定，调用已有模型／Session 能力
    → 保存完整模型结果，执行 Harness 的输出解释代码

若是合法 Shell 请求：
    → Runtime 校验目标并确定环境、执行用户和限制
    → 通过已有执行器实际运行，保存结果或执行引用
    → 明确结果：按本 Harness 的继续定义登记并执行下一 Step
    → 结果不确定：先查询或暂停核对，不直接重做

若是本 Harness 接受的普通最终回答：
    → 保存回答，结束本次推进系列，不登记自动下一 Step

达到限制或遇到不可继续错误：
    → 保存已有结果和停止原因，不把它伪装成任务成功
```

Loop 的选择来自这份 Harness 定义，Runtime 不将它固化为所有策略的默认循环。可以用已有执行器的回调或阶段入口实现；具体绑定方式需核对现有能力，不把伪代码中的步骤冒充已经存在的方法。

任务沙箱中的一次 Shell 生成报告，Runtime Shell 中的另一次 Shell 更新 notes，分别由同一个执行入口按目标位置路由。独立验收读取实际文件，检查总和为 **10**，并核对两次实际环境、执行身份及结果引用。

**事件往返作为同一单 Surface 的附加检查：** 在获授权 Runtime Shell 中构造一条应用通知，通过薄入口提交；在下一次明确的事件输入中读回。该通知不要求命名为 `ready`，也不自动触发通用终态或另一个 Surface。不为了检验事件读写而引入编排拓扑。

### 6.6 由最简 Harness 反查契约是否够用

| 参考 Harness 到达的位置 | Runtime／已有组件必须提供的能力 |
| --- | --- |
| 目录准备并登记后首次推进 | 可验证的登记结果、明确启动入口与 Harness 绑定 |
| 投影模型输入 | 当前 Surface、事件文件与前序反馈的实际可读位置 |
| 模型选择工作位置并提交脚本 | 同一通用执行入口、两套真实环境和受控执行身份 |
| 获取结果并继续 | 可关联的执行结果、由 Harness 决定且可恢复的后续安排 |
| 更新工作面 | 普通文件写入及必要版本保留，不依赖专用内容 API |
| 提交并读回事件 | 服务端校验、明确回执、按约定范围注入可读文件 |
| 暂不继续或重启 | 已保存的继续依据，不依赖旧进程、旧沙箱和旧连接 |

接不上的地方，区分“契约没有定义”“定义已有但实现缺失”“本来就属于策略选择”。只补真实缺口，不为每个步骤新建命令或子系统。

## 7. 验证范围与待定实现

### 7.1 先跑通，再按目标扩大验证

**正常闭环。** 先验证第 6 节的同一单 Surface 样例：模板与 blocks 实际参与上下文，模型使用两套执行环境完成任务，真实报告和 notes 正确，提交的事件能够读回。可以先用固定合法模型响应检查接线，再接真实模型；两者证据分别记录。

**安全与恢复。** 保持 Harness 不变，验证越权路径、伪造执行用户、控制 FD 泄漏、重复请求、回执丢失和执行中断。无法确认时允许明确暂停，不允许静默升级权限或盲目重做。将同一任务停在已保存的推进边界，释放任务进程与沙箱，再从记录继续，检验 Scale to Zero 与长程连续性。

**后续价值验证。** 再加入由代码定义的多 Surface 等待与信息传递，验证条件提前发生、等待期无任务专属进程、重启后唤起；增加多策略对比和实际负载试验。单 Surface 跑通不证明跨 Surface 编排、任意故障、高并发或所有评测策略已经成立。

### 7.2 恢复承诺的验收边界

“故障可发生在任意时刻”，不等于“任意指令处的全部瞬时状态已经持久化”。需要明确保存过的模型结果、文件版本、执行身份、输入位置与后续责任。已经确认的状态不能丢；未收到或未保存的结果不能靠猜测补齐。

如果外部操作已经发生但回执丢失，先按身份查询和核对；缺乏安全重试条件时暂停。事件回放、重新推理、任务重试、文件回退与外部补偿分别处理。

必须核对既有 Session 是否支持相应继续点和结果查询。缺失时补齐必要适配，或者标为未达到该恢复目标，不能把从头重跑冒充无损接续。

### 7.3 尚未定稿的工程选择

| 范围 | 已明确的方向 | 待验证的实现选择 |
| --- | --- | --- |
| 事件底座 | 复用成熟组件，受控发布与文件友好读端 | 具体持久服务、确认、读取、消费与保留配置 |
| Surface | 真实目录、模板和 block files | 模板约定、目录布局、版本捕获及恢复机制 |
| 提交接口 | 接收端校验、回执与不确定结果核对 | `emit` 薄包装或已有接口；内部 FD／socket 等传输 |
| 执行环境 | 双 Shell，Runtime 落实环境与身份 | 隔离产品、用户映射、目录路由、挂载与端点限制 |
| Harness 解释执行 | 外部定义策略，底座承担执行与持久交接 | 与现有模型／Session 的调用粒度、回调和继续点适配 |
| 编排与规模 | 普通代码定义依赖，不靠任务进程长期等待 | 多 Surface 调度、并发接管、性能、保留期与服务指标 |

上表不是要求同时引入一批新设施。优先核对既有组件，只有无法满足明确契约时才增加适配。具体目录名、FD 编号、固定 UID、64 KB 消息上限、每步 Git 提交、全局总序与毫秒级 SLA 均不在本版中冻结。

### 7.4 当前证据状态

本版是依据原稿、后续已明确边界和前轮修订整理的设计文档。它提供最简 Harness 的参考闭环与验收要求，尚未接入真实 Runtime、模型与双 Shell 环境运行，也未执行故障注入、容量或费用测试。

**收敛判据：在声明的任务与故障范围内，最简 Harness 只依赖公开契约即可完成任务、保存继续依据，并在中断后恢复或明确说明不能自动继续的原因。达到这一点，再由新增任务和失败案例驱动扩展，而不是继续凭空增加接口。**

---

## 附录：v5 的整理依据与局部调整

**主稿来源。** 以用户原稿《系统底座（Harness Runtime）》的“目标—价值—原则—核心概念—核心不变量与工程决策—极简 Harness”为主线。核心主目标恢复为“Runtime 与 Harness 彻底拆开，Harness 降级”；三条原则逐字保留，不用接口细则替代。

**后续明确的内容。** Surface 就是模板与 block files 组成的目录；创建和编辑用普通代码，之后登记；事件读取需要 Runtime 提供实际位置；提交可以薄包装，但必须有校验和回执；Runtime Shell 与任务沙箱 Shell 分开，Runtime 决定环境和用户；最简 Harness 不考虑拓扑。

**前轮材料的使用范围。** v3／v4 只保留与上述方向一致的输入、授权、执行查询及恢复边界，不沿用其替换后的目标和原则，不把其中的路径、字段和函数样例认定为已经采纳的接口。本次是来源整理，不新增外部技术调研。

| 原文或前版表述 | v5 的局部处理 |
| --- | --- |
| 不包含任何“如何驱动模型推进”的定义 | 改为“不内置任何……策略定义”，保留“解释 Harness 并执行推进”。 |
| 策略替换完全不侵入底座 | 保留价值，补明“在既有底座契约范围内”。 |
| 等待期资源归零、执行即毁 | 明确任务专属资源范围；进入等待或非执行期释放，连续活跃阶段可保温。 |
| 任意断点无损恢复 | 改为“故障后的无损接续”，已确认状态与恢复边界在正文说明。 |
| 原稿三条原则 | 原文保留；保障对象与验证范围在后续契约中说明。 |
| Surface 是抽象状态服务／必须是 Git 仓库 | 使用后续明确的目录、模板和 blocks 定义，版本技术仍是候选。 |
| 无状态纯函数、三轨 Action、专用业务命令 | 保留策略外置和单步参考，改为显式输入与可恢复继续；不另造内容或业务操作。 |
| `events.jsonl` 就是权威底座与全局状态机 | 仅作为文件视图或导出格式；事件服务、规则解释与推进记录职责分开。 |
| Runtime 只需启动 Harness 的本地程序循环 | 恢复为解释并执行外部推进定义，明确底座承担受控执行与持久交接。 |

**本版的主线不变：推进定义归 Harness，运行责任归 Runtime；由四项底座契约承载多策略、等待期归零与长程可靠性，并以原文三条原则约束实现。**
