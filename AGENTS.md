# 本仓库 Agent 工作约束

开始工作前读取用户当前要求、`SPEC.md`、`docs/architecture.md`、`docs/architecture-target.md`、相关 `docs/contracts/` 和 `docs/development.md`。`SPEC.md` 是产品目标、开发原则与不变量的唯一来源；`docs/architecture-target.md` 描述目标的落地设计及尚未实现的差距；源码、锁文件和无版本前缀的契约描述当前实现。不要把目标设计或现有契约测试通过误报为目标已经实现。

## 必须遵守

- 自顶向下工作：目标与不变量 → 组件边界与失败语义 → 可执行判据 → 实现 → 组件验证 → 组合与系统验证。实现选择不能反向改写目标。
- 修改生产行为前先确定适用范围、契约、正常用例和能够拒绝相关错误的负向用例。组件通过不等于组合或系统通过。
- 独立验收应从契约出发，直接检查真实文件、数据库、协议请求或执行环境，并复跑核心检查。Agent 自述、退出码、绿色截图和测试数量不能单独证明完成。
- 不删除失败用例、不放宽误差、不隐藏跳过、不换成更简单数据，也不通过缩小既定范围制造成功。必要的标准修订要说明理由和影响。
- 未确认的外部效果保持未知，不自动重放；已保存责任不能因进程退出、超时或清理而丢失。不要把应用声明或通用退出状态升级为业务事实。
- 保持 Runtime、Harness、模型组件、Sandbox、Surface、Userspace 与宿主授权的职责和依赖方向。优先复用成熟组件，不新增重复的事件、版本、RPC、模型循环或执行引擎。
- 目标平台是 Unix（macOS/Linux），任务环境是 Linux。平台差异显式处理；不要把开发者机器路径、单一宿主或本机物理身份写成产品依赖。
- 凭据、宿主授权库和部署私有配置留在仓库外，不进入 Git、Work、镜像、测试产物或任务环境。新宿主重新建立物理授权，不复制 inode/device 身份。
- 只实现当前 Work/协议格式。除非产品规格明确要求，不添加兼容解码器或多套生产路径。
- 工作树可能含用户或其他任务的改动。先检查实际状态，保留不相关变更；找不到当前有效证据时只能报告未验证。

## 完成标准

约定范围内的必须项都有与当前版本匹配的直接证据；相关组件、组合和系统层级均已覆盖；重要失败已修复并回归；交付文件与被测版本一致。若使用多 Agent，同一可写工作区只提供角色分离，不构成安全隔离。
