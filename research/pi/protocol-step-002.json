{
  "id": "PI-STEP-BOUNDARY-G2-002",
  "registered_utc": "2026-09-12T19:06:41.059867+00:00",
  "question": "Does the existing public after_tool terminate hook produce a real saved single model-tool boundary, without a second provider call, and allow a fresh process to continue from the original result?",
  "why": "C05/S native drive granularity must not silently redefine the v5 single-step saved/release boundary. G2 root source read found public HookMap.after_tool terminate; scoped experiment tests this existing seam before G3 selection.",
  "source": "packages/agent/src/harness/agent-harness.ts:468-486; actual unchanged runtime tools/boundary/lane",
  "cases": [
    "With external after_tool hook terminate=true, same faux assistant requests real append tool: exactly1provider+1effect, returned result and original tool entry stored.",
    "Fresh process inspect previous operation:0provider/0new effects, exact original result/ref and tool output; oldprocess absent before starting new.",
    "Fresh next operation under same bound external policy: provider sees original tool result, returns ordinary final; total effect remains1.",
    "Control with no terminate hook and identical first reply/tool runs2provider calls to natural final; this rejects a fixture that simply cannot continue."
  ],
  "oracles": "Raw Session JSONL, actual effect file and captured provider context/request count; no product code, real model or permissions added.",
  "stop": "4 normal processes including control, each30s; provider<=2 per process; no changes to prior experiments/protocols or upstream."
}
