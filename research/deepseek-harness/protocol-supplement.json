{
  "id": "DEEP-MECH-02",
  "preregistered_utc": "2026-09-12T18:40:20.310751+00:00",
  "reason": "DEEP-MECH-01 found resume header priority and recoverable invalid open-tail truncation; clarify these two boundaries without altering failed oracle or old batches.",
  "question": "Does an external startsRequestSeries policy become a series header after the loop has logged its first request, and does a bad complete row before an explicit completed-turn boundary reject?",
  "oracle": [
    "Two actual fixed-response calls in same Session/loop, external pre-step flag: raw headers initial then series; response2 receives original transcript plus new user prompt.",
    "Seed interrupted prefix through real backend, append invalid complete row followed by turn/end newline to real file; fresh-process resume rejects corruption and does not invoke model."
  ],
  "fixtures": "Actual same plugins/native flock as protocol01. Two textResponse fixtures. Deliberate malformed physical file is failure input; no implementation mock.",
  "commands": [
    "python3 research/deepseek-harness/supplement.py <fresh-batch-name>"
  ],
  "stop_conditions": "Three phases: series-control, seed, repair, <=60s each; no further expansion if these answer scoped question.",
  "limits": "Does not close original complete-invalid-tail rejection failure. Explicit completed-turn boundary is a narrower fact; no arbitrary power-loss or fsync guarantee."
}
