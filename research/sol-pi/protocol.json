{
  "id": "SOL-MECH-01",
  "preregistered_utc": "2026-09-12T18:26:24.236444+00:00",
  "kind": "exploratory mechanism boundary",
  "upstream_commit": "d7ecfc089944f0d04b80122a0a9a6ca0d786f3d0",
  "properties": [
    "P01",
    "P07",
    "P12"
  ],
  "hypotheses": [
    "H02",
    "H10"
  ],
  "question": "Does actual ObservationPack reduce only projection, preserve raw across fresh-process recall, and validate raw after external mutation?",
  "actual_code": [
    "src/sol-pi/extensions/observation-pack/index.ts",
    "src/sol-pi/extensions/observation-pack/observation.ts",
    "@earendil-works/pi-coding-agent@0.84.2 public SessionManager"
  ],
  "fixtures": "Upstream FakePi only dispatches registered extension context handler/tool; actual SessionManager instead of FakeSessionManager. No AgentSession/provider runner; no model called. UTF8 fixed tool result fixture, not claimed to originate from shell.",
  "oracle": [
    "First two context projections equal original, third smaller with stable obs_id and no middle marker; original Session JSONL retains full exact content.",
    "Fresh worker opens same Session file with actual SessionManager and loads extension anew: pages from obs_recall reconstruct identical UTF8 SHA256/content, offset strictly advances, <=32 pages.",
    "Existing object same byte length but changed bytes: projection keeps full original instead of placeholder, logs hash mismatch.",
    "After successful archive, corrupt object directly and recall in a new process: integrity requires rejection; returned corrupted bytes mean RECALL_INTEGRITY_GAP, never P12 acceptance.",
    "Unknown observation id in correct format rejects; Session raw record remains unchanged.",
    "Candidate sources unchanged; changed target hash rejected by independent observer."
  ],
  "command": "python3 research/sol-pi/experiment.py <fresh-batch-name>",
  "stop_conditions": "One create, restart recall, corrupted recall, corrupted projection, missing recall; each30s, <=32pages. Preserve failed batch.",
  "limits": [
    "No full Pi extension-runner lifecycle or compaction proof; host dispatch fixture.",
    "No power-loss/fsync proof.",
    "Pi dependency0.84.2 differs from research Pi0.85.1; no integration compatibility claim.",
    "Token estimates heuristic; thresholds upstream constants, not lore requirements."
  ]
}
