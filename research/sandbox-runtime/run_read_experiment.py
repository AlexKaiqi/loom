from pathlib import Path
import subprocess,os,sys,json,hashlib,platform,datetime
b=Path(__file__).resolve().parent;o=b/'evidence'/sys.argv[1];o.mkdir(parents=True,exist_ok=False);f=o/'fixture';f.mkdir();(f/'home').mkdir();node='/home/USER/.local/share/lore-tools/node-v24.21.0-linux-x64/bin';env={'PATH':str(b/'.tools/root/usr/bin')+':'+node+':/usr/bin:/bin','HOME':str(f/'home'),'LANG':'C.UTF-8'}
cmd=[node+'/node',str(b/'read_experiment.mjs'),str(f)];r=subprocess.run(cmd,env=env,cwd=f,text=True,capture_output=True,timeout=45);(o/'stdout.jsonl').write_text(r.stdout);(o/'stderr.txt').write_text(r.stderr);(o/'run.json').write_text(json.dumps({'command':cmd,'exit_code':r.returncode,'time_utc':datetime.datetime.now(datetime.timezone.utc).isoformat(),'platform':platform.platform(),'inputs_sha256':{n:hashlib.sha256((b/n).read_bytes()).hexdigest() for n in ['protocol-read-002.json','read_experiment.mjs','run_read_experiment.py']}},indent=2)+'\n');print(r.stdout);print(r.stderr)
