{
  "id": "CODEX-CLIENT-001",
  "registered_at_utc": "2026-09-12T18:22:28.902867+00:00",
  "question": "Does the real TypeScript SDK own a durable host lifecycle, and does run() require an explicit completed turn before returning a final response?",
  "hypotheses": [
    "H02",
    "H08",
    "H10"
  ],
  "properties": [
    "P14",
    "complete-output boundary"
  ],
  "source_commit": "ee6814bfa4889fe9b2b3dcc9cc8bdd91effa8ab8",
  "real_code": "sdk/typescript/src/{codex,thread,exec,outputSchemaFile}.ts compiled by tsx without edits",
  "fixtures": "Explicit fake CLI executable emits fixed JSONL, logs OS process lifecycle and argv. No Rust engine/session persistence/model/provider is simulated as candidate evidence. Fixture stands only at CLI process transport boundary.",
  "cases": [
    {
      "id": "completed",
      "input": "thread.started + agent_message + turn.completed; exit 0",
      "expected": "final answer fixture-answer, nonnull usage, thread ID fixed-thread"
    },
    {
      "id": "resume",
      "input": "fresh wrapper resumeThread(fixed-thread)",
      "expected": "actual child argv contains exec and resume fixed-thread"
    },
    {
      "id": "failed",
      "input": "turn.failed error fixture-failure",
      "expected": "run rejects"
    },
    {
      "id": "malformed",
      "input": "{not-json",
      "expected": "run rejects parse error"
    },
    {
      "id": "nonzero",
      "input": "exit 17 with fixture-stderr",
      "expected": "run rejects mentioning17"
    },
    {
      "id": "truncated",
      "input": "thread.started + agent_message; EOF exit0 without turn.completed",
      "expected": "Observe whether wrapper rejects. Returning answer without completion is a capability gap, never evidence of completed execution."
    },
    {
      "id": "abort",
      "input": "child emits thread.started then waits; AbortController after child started marker",
      "expected": "SDK rejects AbortError and child receives termination; no durable server continuation inferred"
    }
  ],
  "oracle": "Fixed fixture streams plus raw child argv/pid/log files and SDK return/error; observer checks explicit events independent of run() return. Completed vs truncated are paired positive/negative controls.",
  "command": "python3 -B research/codex/run-experiment.py --batch client-001",
  "stop": "Each case subprocess <=15 seconds; preserve errors and return observations rather than modify expected. No real credentials or API calls.",
  "scope": "SDK transport and local child lifecycle only; Rust app-server, rollout durability, real model output and commercial app/cloud are unverified."
}
