{
  "kind": "monitor.condition.set",
  "producer": "external",
  "payload": {
    "monitor_id": "string (required)",
    "predicate": "string (required, human/model readable target condition)",
    "check": "string (optional shell command run in content to evaluate the condition)",
    "deadline_epoch": "integer (optional)"
  }
}
