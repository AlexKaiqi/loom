{
  "kind": "design.unit.frozen",
  "producer": "harness",
  "note": "Derived by the harness from the actual file bytes once a unit is accepted.",
  "payload": {
    "unit_id": "string (required)",
    "file": "string (required, relative to content)",
    "file_digest": "sha256 (required)"
  }
}
