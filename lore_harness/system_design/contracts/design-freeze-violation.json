{
  "kind": "design.freeze.violation",
  "producer": "harness",
  "note": "Derived: bytes of a frozen unit changed without an accepted amendment, or an accepted unit's deliverable is missing.",
  "payload": {
    "unit_id": "string (required)",
    "expected": "sha256 or null",
    "actual": "sha256 or null",
    "reason": "string (optional; present when the file is missing)"
  }
}
