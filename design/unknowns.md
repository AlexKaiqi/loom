# 未知项与证据责任

版本：G0-draft-1。OPEN 表示待处理，不表示系统已经满足。以下事项均保留在完整目标内；本表的阶段分配不豁免正式门槛。

| ID | 问题与已有边界 | 最晚闭合/所需证据 | 是否阻止G0 |
| --- | --- | --- | --- |
| Q01 | Runtime、Harness 与既有 Session 的权威状态和持久责任如何衔接？v5 不允许外层重复局部真相 | G1 先给责任与交接模型；G2 再核对候选源码/恢复接口；具体适配开发前实测 | 否；这是明确的顶层设计问题 |
| Q02 | 同一推进允许在哪些保存边界恢复，如何识别已确认/未确认和安全暂停？ | G1 给恢复边界与故障矩阵；G3 前用真实组件确认持久语义 | 否；不承诺任意指令瞬时状态持久 |
| Q03 | 事件受理、登记、执行与继续责任跨组件时如何不漏交接？ | G1 模型与竞态模拟；G2/G3 真实事件/存储条件验证 | 否；v5 已明确不可丢责任 |
| Q04 | 双 Shell 的执行身份、授权目标解析、挂载、FD/端点与凭据如何隔离？ | G1 威胁和权限抽象；G2 沙箱候选实验；G3 前确定具体威胁范围和拒绝用例 | 否；不能默认任何产品已安全 |
| Q05 | 旧执行者怎样停止或实际失去写能力，如何处理人工编辑和共享 Workspace？ | G1 写入协调模型；G3 对选定机制准备接管、冲突和外部写入用例 | 否；单Surface串行是候选基线，不能豁免失效执行者 |
| Q06 | Surface/Workspace 快照捕获范围、未跟踪文件、产物版本及保留期如何定义？ | G1 定义恢复集合语义；G2 比较版本能力；G3 前固定捕获和保留契约 | 否；不能仅用Git HEAD代替内容覆盖 |
| Q07 | 事件排序、确认、去重和保留具体范围，以及提前事件与等待起点如何定义？ | G1 明确语义与反例；G2 候选实测；G3 前冻结相关配置和契约 | 否；v5 无全局总序要求 |
| Q08 | 哪种模型/Session 适配可支持外部策略、记录引用、工具结果查询和继续？ | G2 固定版本源码/运行实验；G3 独立契约验证；缺能力必须补必要适配或保留未达标 | 否；未预选 dsh/Pi/Codex |
| Q09 | 用户代理的API基路径、5.6-terra标识、流式和工具调用兼容性？ | G2 中只用固定无敏感输入做最小兼容性实验；真实模型阶段用已验证适配 | 否；凭据已具备不等于代理可用 |
| Q10 | 生产Linux发行版/内核/隔离后端、持久服务及依赖版本？ | G1 列需求条件；G2 用可得环境比较；对应G3/G5前锁定环境与证据范围 | 否；只部署Linux已明确，不要求Windows兼容 |
| Q11 | 规模负载、唤起/吞吐/费用指标与正式阈值？ | 先论证目标相关负载及评价方式，可探索测量；对应生产实现和正式验收前形成有依据基线，正式另批验证 | 否；不预设大数字，也不能省略P15 |
| Q12 | 多策略评估的任务集、模型/预算控制、失败统计和副作用隔离？ | G1 定义比较逻辑；G2 评测机制调研；X实施前固定评估合同 | 否；比较范围必须可复现 |
| Q13 | Grok Bot原项目是哪一个？ | G2 保留三个候选及未确认身份，依原清单选择有问题依据的研究深度，不伪称确认 | 否；不决定Runtime根本目标 |
| Q14 | 12主项目及专项如何分配研究深度、实验和采用条件？ | G2 调研计划按原清单逐项履行；候选失败或不可运行需证据和未完成状态 | 否；清单范围已确定 |
| Q15 | 独立验收运行器如何防止实现者改写判据、完成状态及原始证据？ | G1 先划开发验收与产品事实责任；验收设施实施前定义威胁边界，实测有效性；共享同一用户目录不声称防篡改 | 否；目标是独立证据，不能靠再添自述 |

所有 Q 项初始 OPEN。闭合记录应说明决策依据、反例、适用条件和受影响证据；需要多阶段证据的项目可先闭合设计子问题，运行语义仍保持待验证。G0本身不需要用户重做这些工程判断。

2026-09-13 独立边界审查修正：Daytona 的历史参考定位仅限制版本和能力范围，不免除原清单§9的相关可复现实验；见 R014。先前 g0-scope-review.md 的“只做历史结构对照”是待纠正意见，不能作为降标依据。
