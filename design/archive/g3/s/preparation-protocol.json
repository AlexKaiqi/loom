{
  "id": "S-PREP-PUBLIC-001",
  "status": "PREREGISTERED_PREPARATION_NOT_COMPONENT_ACCEPTANCE",
  "scope": "Fixed actual Pi public API with shipped faux response. No provider/key, production bridge, isolation or durability claim.",
  "questions": [
    "Does public Session.setValue retain custom binding under a separate namespace?",
    "Does before_tool reading original Session reject an entire multi-tool batch before any tool effect, after_tool stop plus blocked terminate avoid another provider?",
    "What exact original evidence is present at provider callback and unsafe tool.execute?",
    "Does public restore+drive of orphaned assistant.effect_pending call provider again or settle interruption under original ID?"
  ],
  "fixed_inputs": {
    "pi_commit": "71dca871bc80b6bc97be37f0ca3189399d651fff",
    "node": "24.21.0",
    "loader": "research/repos/pi/node_modules/tsx/dist/loader.mjs",
    "tsconfig": "research/pi/tsconfig.json"
  },
  "trajectories": [
    {
      "id": "PREP-SINGLE",
      "actions": [
        "fresh Session/public custom binding",
        "accept op-1",
        "drive one complete native tool",
        "copy original raw intent in provider callback and tool delegate",
        "public getResult"
      ],
      "expected": [
        "binding survives read",
        "provider callback sees assistant.effect_pending with reserved responseEntryId",
        "tool delegate sees original complete assistant entry and effect_pending tool call",
        "one provider/one actual append/result tool content retained"
      ]
    },
    {
      "id": "PREP-MULTI",
      "actions": [
        "fresh Session",
        "fixed two native tools",
        "before_tool read current original assistant; block each with terminate=true if count!=1",
        "drive"
      ],
      "expected": [
        "zero actual effects",
        "one provider request",
        "raw two-tool assistant retained",
        "operation settles without second model"
      ]
    },
    {
      "id": "PREP-PENDING",
      "actions": [
        "fresh Session",
        "accept",
        "provider callback copies actual Session then exits before response",
        "new process restore/query",
        "public drive existing op"
      ],
      "expected": [
        "first has assistant.effect_pending reserved ID",
        "query starts zero provider",
        "drive starts zero provider",
        "raw recovered assistant uses original reserved ID and interrupted/error, not a normal answer"
      ]
    },
    {
      "id": "PREP-SAVED",
      "actions": [
        "new process read saved PREP-SINGLE original Session"
      ],
      "expected": [
        "zero provider/tool",
        "getResult equal original",
        "raw bytes unchanged"
      ]
    }
  ],
  "negative_control": {
    "id": "PREP-NO-GUARD",
    "actions": [
      "same two-tool response without batch guard or after_tool stop"
    ],
    "expected": [
      "two real fixture effects",
      "second provider request occurs"
    ]
  },
  "stops": {
    "process_timeout_seconds": 30,
    "max_processes_total": 7,
    "max_provider_requests_per_process": 2,
    "network": "none used",
    "fixture_file_limit_bytes": 1048576,
    "basis": "Finite declared requests and small JSONL source probes; watchdogs are experiment safety limits, not product latency/size SLAs."
  },
  "failure_policy": "Save all raw output/candidate JSONL and actual effects; unexpected observed behavior remains a failed prediction and blocks relevant claim. Never change old run or protocol to green.",
  "separation": "Probe author cannot grant G3. Synthetic response is candidate plumbing evidence only. Exact protocol hash recorded before first execution."
}
