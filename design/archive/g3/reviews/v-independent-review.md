当前裁定：**PASS，仅 V 的 G3 验收准备范围**。以下初审问题保留为历史，修订复核见文末。

# V 与 M/X 先行范围独立审查

裁定：V 准备 **CHANGES_REQUIRED**；M/X 声明范围与资源预算适合作为先行基线，待两处接口澄清及 G5 前实际 driver 冻结。本报告不通过任何 Runtime 功能，不替代244项完整语义审查。

- **VR01（阻断）**：`validation/components/v/test_gate.py:12/38` 全部夹具只有一批、一个检查。只执行首项、忽略真实后续失败的实现仍可能通过；V01也未核返回的完整覆盖、计数和逐项结果。需在原用例内真实多批/多检查、非首项错误传播、精确调用与完整结果；空全集和重复身份不能被当作完整性通过。
- **VR02（阻断）**：`validation/components/v/run_contract.py:17/25` 导入后只存入口源码。真实导入helper变动、旧pyc/当前文本失配未被绑定，仍可能source_unchanged。需捕获实际有限包源码闭包并保证执行该版本，添加对应窄拒绝控制。无需通用平台。

独立实跑：`v-independent-preparation-001`保存命令、stdout/stderr与输入SHA。缺组件实际MISSING/退出2/0例；只返回PASS字样的坏入口实际FAIL/退出1/14例。两者证明入口不会把这两个控制误算成功，不能填补上述未覆盖路径。

M/X仍包括多Surface时序、两策略并存/影子隔离、1至10000等待阶梯及1/2/4/8活跃全组件路径，M未充当任务终点。模型样本固定、全部失败保存，不声称统计优越。每slot三对象预留768MiB，八slot为6GiB，和S/X分阶段资源合同无矛盾；CPU/RSS/磁盘及吞吐数值均先行约束，尚无系统测量结果，因此不能当容量证据。

两处澄清：system合同输入关联编号应为RE03；先接受下一责任不等于原holder尚在时允许下一写者或抢先冻结新input，须在原seal/F安装/协调闭合后取得匹配base。G5组合实现前仍须冻结实际算术混合工作负载、逐项collector/采样及独立负控。当前不要求尚未实现系统的运行证据。

原文、测试、输入和报告SHA见同名JSON。原问题及旧独立运行保留；修订后只复核受影响范围。

## 原反例的修订复核

VR01已关闭：两个实际run目录/三个check，V01逐项核真实回调次数及返回的原观察、断言、facts和完整要求映射；V02/05/11/12的非首项错误不能被首项成功遮住，V03保留空全集/重复身份拒绝。14例未删减。最后“只有ID但无原观察值”的空壳输出缺口也已补确切比较。

VR02已关闭：import前保存选定完整包的.py源码，从不含pyc的新快照执行，并比较原包和执行快照；独立实际八项控制复现helper变化拒绝、伪造时间戳/大小有效旧pyc内9而源码1时实际执行1。限定标准库与已捕获包，未声称跟踪任意外部依赖。

独立新批`v-independent-preparation-002`：缺组件MISSING/退出2/0例；always_green被14例入口拒为FAIL/退出1；source snapshot八控通过。原批和原报告副本保留，没有把缺组件或错误夹具当组件PASS。系统RE03编号及受理后执行资格已澄清，原数值与M/X范围未降标。

本裁定允许继续对应V实现前冻结，不证明V自身能力、系统运行/容量或244项最终覆盖。G5实际collector、逐项工作负载与采样须在相应组合实现前独立冻结。当前源/证据完整SHA在同名JSON的revision_review。
