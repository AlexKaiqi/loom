# Runtime event reference seam

Pre-implementation gap: R reference_checker is synchronous, while original E/NATS APIs are asynchronous. Reuse original nats-py 2.15.0 JetStream get_msg in one shared reader thread/connection; no event history, ack policy or delivery state is reimplemented. E remains owner of publications/receipts and fixed input files; R remains responsibility/selector owner.

API: JetStreamReader(url).open()/read(stream, sequence)/close(), bounded timeout/no reconnect, one owned loop/thread/client, direct read of original JetStream message by exact positive sequence. EventReferences(control, events, reader)(ref, purpose, expected) supports only input/receipt/observation, returns false on unavailable/invalid sources; other owners delegated elsewhere. Uses actual R row, E _facility/_wrapper/_negative and input_files.validate, exact expected scope. Input also compares original seq/message bytes to every actual event_refs/events.jsonl entry. Reads never publish application events, ACK consumers, change R or repair original files. Runtime invokes E.prepare_input/lookup_input before plan assembly; this seam does not choose a new H.

Before implementation execute validation/event_authority_probe.py --batch missing-001 using fixed actual NATS binary and original R/E modules; missing adapter is MISSING/0. Finite cases EA01 actual positive delivery+full selector input and readonly repeated resolution, EA02 all exact binding/locator changes reject, EA03 raw input same-length corruption reject and no repair, EA04 original NATS seq absent rejects, EA05 genuine server definite rejection receipt exact original raw and wrong scope/hash reject, EA06 original R surface registration observation and forged owner/source/revision reject, EA07 fresh shared-reader reopen and exact original query/no new event or R/file mutation, close has no live owned thread/client. Bad always-green adapter must fail negatives. No Docker, model, S or system PASS claim.

Runtime code never imports validation, never accepts a caller-created success document as an original event, and never converts reading/ACK into parent responsibility completion. Limits and original E finite file/discard-new/noTTL/single-replica profile unchanged.
