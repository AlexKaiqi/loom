# Runtime emit bridge and bootstrap route: finite independent checks

Sources: runtime-assembly.md RA03 and entry-contract.md, plus emit.md original unknown/next-input rules. This registration precedes execution. No product edits, Docker, model, or whole Runtime construction. RA01 start/query zero-effects and M01 remain root assembly obligations.

- EB01: true Runtime._emit_from_worker before open and after true close rejects `invalid`; no resolver invocation, R request changes, application PUB or input/X-journal bytes. Main event-loop synchronous call also rejects without writes.
- EB02: true Runtime.open + existing EventService/R + real local NATS and JetStreamReader; an actual Python worker calls the bridge. The actual EmitService coroutine must run on the original Runtime event loop/thread, preserve full arguments, publish exactly once, and yield the exact original NATS event in the next explicit E input. Repetition uses the original ID and does not PUB again; the prior frozen input stays unchanged. Same original E client identity is retained.
- EB03: real E after_dispatch_before_publish checkpoint blocks after original R issued row. A wrapper around the real concurrent Future only shortens result wait from requested 30 s to 1 s; cancellation delegates to that same Future. Bridge must return delivery_unknown; original issued responsibility remains, no PUB occurred, subsequent E query and same-ID Emit submission stay UNKNOWN and do not PUB. This validates the timeout path, not the wall-clock 30 s duration.
- BR01: directly call actual bootstrap._References with counting trusted owners. A permissive host must never be called after specialized tool/publication/provider refusal, missing owner, or exception; unknown receipt rejects. Each routed positive must use the same chosen owner, and generic host calls remain reachable. This is routing verification, not proof that test owner callbacks are production authorities.

Method-scope assembly uses object.__new__(Runtime) solely to supply real E/R/reader/Journal fields without constructing Engine. Runtime methods themselves are unmodified. Archive Source is the existing emissions fixture: actual tar bytes, explicitly X/F-shaped source references, not proof of a real tool/F publication. Candidate and validation Python files are copied before imports, with source equality and actual loaded paths checked; current session_sources.py is intentionally excluded because bootstrap only imports it inside uncalled assemble(). NATS and Python are the fixed installed tools, bound by actual path/hash. Maximum one local NATS server, 45 s case sequence / 90 s process. All failures and cleanup retained.


File-thread integration revision: bootstrap now constructs RuntimeFiles with its original ControlStore, and Runtime.open binds that same main loop before opening reader/E. The earlier method-only fixture omitted the mandatory `files` attribute of real Runtime construction. It now supplies actual RuntimeFiles over a real F Git/control directory with explicitly rejecting file authority callbacks (no F content action is required here). EB02 asserts same-loop/same-R binding; EB01 adds that F directory to the no-write observations. The original batch/source remain immutable. This is fixture correction to the original dependency shape, not relaxed product behavior. Real F capture/install/query behavior is covered separately by file-thread-independent-001.
