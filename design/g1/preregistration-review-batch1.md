# G1 独立预注册审查

审查人：long_task_drift（独立于契约与用例作者）。审查范围：冻结前的文档、预期与反例，未编写或运行模拟。日期：2026-09-13。

## 批次一裁定

**DUR 28例与STR 3例：READY_TO_FREEZE。BND：UNVERIFIED，另行审查。G1运行及总门槛：UNVERIFIED。** 本裁定允许在根Agent将下列确切版本及本审查纳入冻结清单后，仅实现不依赖BND的DUR/STR有限模型；不能当作现实系统或模拟运行通过。共同契约或已审案例后续变化须先检查影响，不可沿用旧摘要静默实现。

已逐例读取DUR/STR的初态、动作、断言、错误机制和现实差距，并直接对照v5原始目标、顶层契约、路线审查与模拟协议。不是仅凭字段或摘要判定覆盖。

- DUR覆盖传输/接受/登记/启动区分；稳定身份重试与冲突；结果已确认但后续责任尚未移交的崩溃；已接受后续任务的恢复；所有易失进程消失后的已确认状态恢复；执行未知的查询/暂停；等待前、中、后交错及通知丢失；读视图不消费；重复事件；保留与归档；等待释放任务专属资源；停止请求与真实停止；事实回放不重做；已声明过期历史。
- DF01由DUR-007与DUR-027形成继续/终结的相反判据，不能将普通工具返回统一视为终态。DF02/AR-01的契约修复已直接复核；DUR-028要求恢复复用已接受的d1=A/op2，并拒绝同一身份改为B/op3。外部策略非恒定不再被默认排除。
- STR覆盖同一内核接受continue/stop/wait三种外部决定、截断/畸形/请求未完成不冒充最终输出，以及预算停止不等于业务成功或Surface终态。输出标志是独立有限输入，不能冒充真实解析器测试。
- 动作为已登记op+args，预期路径/比较器/检查时点可解释；DUR的预期ID与must_violate引用一致。错误机制必须改变共享转移并触发对应因果断言，不能修改expected、按案例ID返回答案或以崩溃代替反例被检出。
- 观察规则把接收方接受记录、目标效果、真实停止/资源计数等作为环境夹具，不能仅相信Runtime声称成功。确认内容可恢复、责任覆盖与计数须由记录关系推导。观察器还须拒绝漏例、漏断言、缺轨迹、未知操作及版本不符。
- 有限活性有公平驱动与步数边界；耗尽不能记PASS。已确认状态缺失不能用永久暂停掩盖。合法未知暂停同时保留事实和未结责任；正向完成/释放/恢复案例约束“永远暂停”的伪安全实现。

## 证据边界与保留发现

AR-01原始缺陷及修订复核保留于alternatives-review.md；当前只关闭设计语义缺陷，未关闭真实能力债务。持久确认、可靠查询与幂等范围、实际授权/撤权、完整版本、外部回调副作用控制、无旧进程恢复及Session适配均须G2/G3的真实组件证据；模型赋予的能力不能反向证明组件具备该能力。P14效果和P15容量不从有限模型推导。

BND此前读到的draft1动作/断言仍为自然语言，故当时不具备协议要求的可执行预注册格式。转换后的新版本尚待本审查者逐例复核；不将作者转换完成或根Agent摘要视作独立通过。BND不阻止有明确独立边界的DUR/STR批次，亦不从G1必需范围删除。

## 批次一输入摘要

| 文件 | SHA256 |
|---|---|
| top-level-contracts.md | 5644cd02f5d6625690ba8ff9e90e7e4085606eba44eb532eb4c2de34314adeb4 |
| simulation-protocol.md | ccc770ccc875b0764cc14b64ecb755441a093924b70c9805e73212131a79a350 |
| mechanism-decisions.md | c50f39f84379b6527edfbeb807291a75ff7a1511a63b2495f159ed0f1d3afaa0 |
| cases-durability.json | fcd8f28d3e9eb245cef55f5716a404e458fff0d9ec3f5f7c75906e2490f3f861 |
| cases-strategy.json | c29b132edd3a8c09ba399cb3cd3c1183ecc83cfe7e362f7c9ae4342b7ee50a60 |
| alternatives-review.md | bf92f7b4e22c35ff024a1bd87bffa70979daa9e4f77f580d2fe9c74329835346 |

以上路径均相对design/g1；冻结者应记录实际Git提交和本报告摘要。旧BND draft1审读版本摘要为451c9fd8879545b2995b5f678379b152f5e342c9e2a2f1e205a053b9fa583ff3，仅记录历史未通过状态，不进入批次一实现输入。
